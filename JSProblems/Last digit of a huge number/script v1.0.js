lastDigit();

function lastDigit(a, b) {
    // for (let i = 1; i < 40; i++) {
    //     console.log(i, (BigInt(123)**BigInt(i)).toString().slice(-1));
    // }
    console.log((BigInt(2)**BigInt(31)**BigInt(4)).toString());
}